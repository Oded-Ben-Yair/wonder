{
  "name": "wonder-monorepo",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "gateway",
    "engine-azure-gpt5",
    "engine-basic",
    "engine-fuzzy"
  ],
  "scripts": {
    "dev": "cd gateway && npm run dev",
    "start": "cd gateway && npm start",
    "test": "echo 'Running tests across all workspaces'",
    "probe": "bash scripts/probe.sh",
    "smoke": "bash scripts/csv-smoke.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}